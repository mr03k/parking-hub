apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: haproxy-in-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/mr03k/parking-hub.git
    targetRevision: HEAD
    path: argocd-main/resources/infra/haproxy
  destination:
    server: https://kubernetes.default.svc
    namespace: gateway-resources
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
